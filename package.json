{
  "name": "photography-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-static.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "markdown-it": "^15.0.0"
  }
}
